@model ProjetoDengue.Models.Campo

@{
    ViewBag.Title = "evc";
}

<h2>evc</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Campo</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.tamanhoX, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.tamanhoX)
                @Html.ValidationMessageFor(model => model.tamanhoX)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.tamanhoY, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.tamanhoY)
                @Html.ValidationMessageFor(model => model.tamanhoY)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
